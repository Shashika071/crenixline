version: "3.8"

services:
  mongo:
    image: mongo:6.0
    container_name: mongo-rs0
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - mongo_data:/data/db
    networks:
      - app-network

  backend:
    build: ./backend
    container_name: backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    depends_on:
      - mongo
    networks:
      - app-network
    expose:
      - "5000"

  frontend:
    build: ./frontend
    container_name: frontend
    restart: unless-stopped
    networks:
      - app-network
    expose:
      - "80"

  nginx:
    image: nginx:alpine  # Use alpine for better permission handling
    container_name: nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./frontend/dist:/usr/share/nginx/html:ro
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - frontend
      - backend
    networks:
      - app-network
    user: "nginx"  # Explicitly set user

volumes:
  mongo_data:

networks:
  app-network:
    driver: bridge